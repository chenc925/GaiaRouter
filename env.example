# GaiaRouter 环境变量配置示例
# ============================================
# 使用说明：
# 1. 复制此文件: cp env.example .env
# 2. 修改.env文件中的配置为你自己的实际值
# 3. 不要提交.env文件到版本控制系统
# ============================================

# ============================================
# 数据库配置（必需）
# ============================================
# 请根据你的实际环境配置数据库连接信息
#
# 选项1: 使用阿里云RDS（推荐生产环境）
#   - 在阿里云控制台获取数据库连接信息
#   - 确保网络白名单已配置
#
# 选项2: 使用本地MySQL（开发测试）
#   - 安装MySQL: brew install mysql (macOS)
#   - 启动服务: brew services start mysql
#   - 创建数据库: mysql -u root -e "CREATE DATABASE gaiarouter;"
#   - 配置如下:
#     DB_HOST=localhost
#     DB_USER=root
#     DB_PASSWORD=your_local_password

# 数据库主机地址（必需 - 请修改为你的数据库地址）
DB_HOST=your-database-host.com
# 本地开发示例: localhost
# 阿里云RDS示例: rm-xxxxxx.rwlb.rds.aliyuncs.com

# 数据库端口（MySQL默认3306，PostgreSQL默认5432）
DB_PORT=3306

# 数据库用户名（必需 - 请修改）
DB_USER=your_db_user

# 数据库密码（必需 - 请修改）
DB_PASSWORD=your_db_password

# 数据库名称
DB_NAME=gaiarouter

# 连接池大小
DB_POOL_SIZE=10

# 最大溢出连接数
DB_MAX_OVERFLOW=20

# ============================================
# AI模型提供商API Key配置
# ============================================
# OpenAI API Key（可选，如果使用OpenAI模型）
OPENAI_API_KEY=sk-your-openai-api-key

# Anthropic API Key（可选，如果使用Anthropic模型）
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key

# Google API Key（可选，如果使用Google模型）
GOOGLE_API_KEY=your-google-api-key

# OpenRouter API Key（可选，如果使用OpenRouter模型）
OPENROUTER_API_KEY=sk-or-XXX

# ============================================
# 服务器配置
# ============================================
# 服务器监听地址
HOST=0.0.0.0

# 服务器端口
PORT=8000

# 调试模式（true/false）
DEBUG=false

# 日志级别（DEBUG, INFO, WARNING, ERROR, CRITICAL）
LOG_LEVEL=INFO

# ============================================
# 请求配置
# ============================================
# 请求超时时间（秒）
REQUEST_TIMEOUT=60

# 最大重试次数
MAX_RETRIES=3

# ============================================
# 安全配置（可选）
# ============================================
# JWT密钥（如果使用JWT认证）
# JWT_SECRET_KEY=your-secret-key

# CORS允许的来源（多个用逗号分隔）
# CORS_ORIGINS=http://localhost:3000,https://yourdomain.com

# ============================================
# 其他配置（可选）
# ============================================
# 时区设置
# TZ=Asia/Shanghai
